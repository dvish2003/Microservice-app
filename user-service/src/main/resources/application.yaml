server:
  port: 8081

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: userdb
  application:
    name: USER-SERVICE


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true